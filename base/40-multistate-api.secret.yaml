# base/40-multistate-api.secret.yaml — placeholder Secret (stringData).
# W6 D3 replaces with External Secrets Operator + IRSA.
apiVersion: v1
kind: Secret
metadata:
  name: multistate-api-secrets
  namespace: multistate-dev
  labels:
    app.kubernetes.io/name: multistate-api
type: Opaque
stringData:
  SPRING_DATASOURCE_PASSWORD: local-dev-placeholder
