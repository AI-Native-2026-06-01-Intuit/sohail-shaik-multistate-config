# multistate-agent-svc/base/40-secret.yaml
# Placeholders only — replace via sealed-secrets / ExternalSecrets / kubectl
# create before first sync. Never commit real keys.
apiVersion: v1
kind: Secret
metadata:
  name: multistate-agent-svc-secrets
  namespace: multistate-svc
  labels:
    app.kubernetes.io/name: multistate-agent-svc
type: Opaque
stringData:
  MULTISTATE_AGENT_POSTGRES_URL: "postgresql://multistate:replace-me@postgres.multistate-dev.svc.cluster.local:5432/postgres"
  MULTISTATE_AGENT_RAG_POSTGRES_URL: "postgresql://multistate:replace-me@postgres.multistate-dev.svc.cluster.local:5432/postgres"
  ANTHROPIC_API_KEY: "sk-ant-replace-me"
  LANGSMITH_API_KEY: "replace-me"
